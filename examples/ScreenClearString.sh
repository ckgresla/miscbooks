#!/bin/bash
# Clear out a screen with this string (could print from python or some scripting lang) -- effectively same as `clear` command or ctrl+l keybinding

echo '\x1bc'
#run the above at the command line
